name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v1
    - name: prepare npm
      run: npm install
    - name: test
      run: npm test
      env:
        CI: true
