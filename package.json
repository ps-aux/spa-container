{
  "name": "spa-server",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --watch",
    "build": "docker build . -t xxx",
    "run": "docker run --rm --name spa-server -p 80:80 xxx",
    "rnb": "npm-run-all build run"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^22.4.3",
    "npm-run-all": "^4.1.2"
  }
}
